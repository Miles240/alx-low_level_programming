#include <stdio.h>

/**
 * main - Entry - point
 *
 * Return: Always 0
*/

int main(void)
{

    int a;
    int b;
    int c;
    int d;

    for (a = 48; a <= 57; a++)
    {
        for (b = a + 1; b <= 57;b++){
            putchar(a)
        }
    }
    putchar('\n');
    return (0);
}