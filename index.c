#include <stdio.h>
#include <string.h>

int main (void){

    char *str = "1234567";
    int len = strlen(str- 1) / 2;
    int i;
    for (i = 0; i <= len; i++){
        printf("%d", len);
    }

    return (0);
}